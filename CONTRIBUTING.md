# دليل المساهمة في مشروع mgd-gi

شكراً لاهتمامك بالمساهمة في **محرك المعرفة المكاني**! نرحب بجميع أشكال المساهمات.

## 🤝 كيفية المساهمة

### 1. Fork المستودع
```bash
git clone https://github.com/YOUR_USERNAME/mgd-gi.git
cd mgd-gi
```

### 2. إنشاء فرع للميزة الجديدة
```bash
git checkout -b feature/your-feature-name
# أو للإصلاحات
git checkout -b fix/bug-description
```

### 3. عمل التعديلات
- اتبع نمط الكود الموجود
- أضف تعليقات واضحة
- اختبر التغييرات محلياً

### 4. Commit التغييرات
```bash
git add .
git commit -m "وصف واضح للتغيير"
```

استخدم الرموز التعبيرية:
- 🚀 ميزة جديدة
- 🐛 إصلاح خطأ
- 📚 توثيق
- 🎨 تحسين التصميم
- ⚡ تحسين الأداء

### 5. Push والفتح Pull Request
```bash
git push origin feature/your-feature-name
```

ثم افتح Pull Request على GitHub.

## 📋 معايير الجودة

### الكود
- استخدم TypeScript
- اتبع معايير ESLint
- أضف اختبارات للميزات الجديدة
- تأكد من عدم وجود أخطاء TypeScript

### التوثيق
- حدّث README إذا لزم الأمر
- أضف تعليقات للكود المعقد
- وثّق الـ APIs الجديدة

### الاختبار
- اختبر على iOS و Android
- اختبر دعم العربية (RTL)
- تحقق من الأداء

## 🎯 أولويات المساهمة

### عالية الأولوية
- إصلاح الأخطاء الحرجة
- تحسينات الأداء
- دعم اللغات الجديدة
- توثيق شامل

### متوسطة الأولوية
- ميزات جديدة
- تحسينات UX
- إعادة تنظيم الكود

### منخفضة الأولوية
- تحسينات تجميلية
- تحديثات التعليقات

## 🐛 الإبلاغ عن الأخطاء

عند فتح Issue:
1. استخدم عنواناً واضحاً
2. صف الخطأ بالتفصيل
3. أرفق خطوات إعادة الإنتاج
4. أضف لقطات شاشة إن أمكن
5. ذكر إصدار التطبيق والنظام

## 💡 اقتراح ميزات جديدة

1. تحقق من المشاكل المفتوحة أولاً
2. صف الميزة بوضوح
3. اشرح الفائدة المتوقعة
4. أرفق أمثلة أو تصاميم

## 📝 معايير الكود

### TypeScript
```typescript
// استخدم أنواع واضحة
interface NodeData {
  id: string;
  type: NodeType;
  x: number;
  y: number;
}

// أضف تعليقات للدوال المعقدة
/**
 * حساب المسافة بين نقطتين
 * @param x1 إحداثي X الأول
 * @param y1 إحداثي Y الأول
 * @param x2 إحداثي X الثاني
 * @param y2 إحداثي Y الثاني
 * @returns المسافة بين النقطتين
 */
function distance(x1: number, y1: number, x2: number, y2: number): number {
  const dx = x2 - x1;
  const dy = y2 - y1;
  return Math.sqrt(dx * dx + dy * dy);
}
```

### React Components
```typescript
// استخدم React.FC مع Props interface
interface MyComponentProps {
  title: string;
  onPress?: () => void;
}

export const MyComponent: React.FC<MyComponentProps> = ({ title, onPress }) => {
  return (
    <View>
      <Text>{title}</Text>
    </View>
  );
};
```

## 🧪 الاختبار

```bash
# تشغيل الاختبارات
pnpm test

# التحقق من TypeScript
pnpm check

# تشغيل الـ Linter
pnpm lint

# تشغيل التطبيق محلياً
pnpm dev
```

## 📞 التواصل

- **Issues**: للإبلاغ عن الأخطاء واقتراح الميزات
- **Discussions**: للأسئلة والنقاشات العامة
- **Email**: ai@manus.im

## 📜 الترخيص

بالمساهمة في هذا المشروع، أنت توافق على أن تكون مساهماتك مرخصة تحت MIT License.

---

شكراً لمساهمتك! 🙏
